<turbo-frame id="modal_content">
  <%# O fundo do modal já é controlado na página principal (show.html.erb). %>
  <%# Apenas precisamos de ajustar as cores do texto aqui. %>
  <div class="p-6">
    <h3 class="text-2xl font-bold text-gray-900 dark:text-gray-100 mb-4">Editar Anotação</h3>
    
    <%# Esta partial renderiza o formulário em si. %>
    <%= render 'form', note: @note %>
  </div>
</turbo-frame>
